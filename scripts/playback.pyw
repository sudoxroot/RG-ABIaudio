from tkinter import *
from tkinter.ttk import *
master = Tk()
master.geometry("500x400")
master.title("RG-ABI audio Playback")
master.iconbitmap("./scripts/mainIcon.ico")


label = Label(master, text ="This is still in development!")
label.pack(pady = 10)


# mainloop, runs infinitely
mainloop()
